{% extends 'base.html.twig' %}

{% block title %}New Liste{% endblock %}

{% block body %}
    <h1>Créer une nouvelle liste</h1>

    {{ include('liste/_form.html.twig') }}

    <a href="{{ path('create_list', {'id': 1, 'var' : 'augmenter'}) }}" class="btn btn-info">Créer ma liste</a>

    <a href="{{ path('app_liste_index') }}">back to list</a>
{% endblock %}
